#include "main.h"
/**
 * add - function that adds two integers and
 * @a: int to be added to b
 * @b: int to be added to a
 * Returns: sum of a and b the result.
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
